plugins {
    id 'java'
}

group = 'com.root101.module.admin'

version = '1.9.9.SNAPSHOT.20210101'

repositories {
    jcenter()
    mavenCentral()
}

dependencies {
    compile group: 'org.springframework', name: 'spring-web', version: '5.2.9.RELEASE'
    implementation project(':utils-core')
    
    implementation project(':clean-core')
}

